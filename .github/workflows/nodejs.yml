name: CI

on:
  push:
    branches: [ master, 12.x ]

  pull_request:
    branches: [ master, 12.x ]

jobs:
  Job:
    name: Node.js
    uses: node-modules/github-actions/.github/workflows/node-test.yml@master
    with:
      os: 'ubuntu-latest'
      version: '14, 16, 18, 20'
